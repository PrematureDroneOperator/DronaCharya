# Jetson Nano base runtime (Python 3.6-compatible)
# OpenCV and NumPy are expected from JetPack system packages.
PyYAML            # PINNED for Python 3.6 compatibility (fixes cython_sources error)
pymavlink
pyserial           # required by telemetry/radio_bridge.py (radio/Pixhawk serial MAVLink bridge)
dataclasses 
